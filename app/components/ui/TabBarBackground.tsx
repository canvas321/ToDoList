// TabBarBackground.tsx (fallback)
import React from "react";
import TabBarBackgroundIOS from "./TabBarBackground.ios";
// Optionally, if needed for Android, you could create TabBarBackground.android.tsx as well.

const TabBarBackground = () => {
  return <TabBarBackgroundIOS />;
};

export default TabBarBackground;
